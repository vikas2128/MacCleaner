name: MacCleaner
options:
  bundleIdPrefix: com.vikas
targets:
  MacCleaner:
    type: application
    platform: macOS
    deploymentTarget: "12.0"
    sources: [Sources]
    info:
      path: Info.plist
    entitlements:
      path: MacCleaner.entitlements
